// scenes/02-Identity/Identity.content.js
//
// Copy only, kept separate from layout per Architecture §2. Per the Scene
// Map: a short editorial statement about mindset, not occupation — no
// titles, no skills, no technology names, no achievements. Those stay
// withheld until later scenes (Milestones, Work, Creative Side).

export const identityContent = {
  statement:
    'He builds things to understand them — then keeps building, because understanding was never really the point.',
  supportingLine:
    'Everything that follows started the same way: as something made for no one but himself.',
};

export default identityContent;