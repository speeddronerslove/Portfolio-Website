# scenes

One folder per scene, 1:1 with the approved Scene Map: 01-Arrival through
08-Contact. Each will hold a `SceneName.jsx`, a `SceneName.content.js`, and
an `index.js`, composed from `components/primitives` and `components/motion`
inside a `SceneWrapper`. Empty until scene-building begins.
