# components/motion

`RevealOnScroll`, `ParallaxLayer`, `ImageExpand`, `FadeBlend` — composed
from variants in theme/motion.js. Not built yet — foundation phase only
includes the token/motion/context/orchestrator layer. See Architecture §2–3.
